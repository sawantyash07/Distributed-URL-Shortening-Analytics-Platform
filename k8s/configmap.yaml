apiVersion: v1
kind: ConfigMap
metadata:
  name: url-shortener-config
  namespace: url-shortener
data:
  SERVER_PORT: "8080"
  FRONTEND_URL: "https://url-shortener.example.com"
  SHORT_BASE_URL: "https://api.url-shortener.example.com/r"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
